{
  "buildCommand": "apt-get update && apt-get install -y pkg-config build-essential libxml2-dev libxslt-dev zlib1g-dev && bundle config build.nokogiri --use-system-libraries && bundle install && jekyll build",
  "installCommand": "bundle install",
  "framework": "jekyll",
  "outputDirectory": "_site",
  "build": {
    "env": {
      "BUNDLE_FORCE_RUBY_PLATFORM": "true",
      "NOKOGIRI_USE_SYSTEM_LIBRARIES": "true",
      "PKG_CONFIG_PATH": "/usr/lib/x86_64-linux-gnu/pkgconfig:/usr/lib/pkgconfig"
    }
  }
} 