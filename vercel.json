{
  "buildCommand": "bundle config build.nokogiri --use-system-libraries && bundle install && jekyll build",
  "installCommand": "bundle install",
  "framework": "jekyll",
  "outputDirectory": "_site",
  "build": {
    "env": {
      "BUNDLE_FORCE_RUBY_PLATFORM": "true"
    }
  }
} 